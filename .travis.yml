language: python
python:
  - "3.7"
install:
  - travis_retry pip install -r test/requirements.txt
  - travis_retry pip install coveralls
  - travis_retry pip install -e .
script: py.test --codestyle --mypy --cov=pymap
after_success:
  - coveralls
branches:
  only:
    - master
